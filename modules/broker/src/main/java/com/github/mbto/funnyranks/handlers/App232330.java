package com.github.mbto.funnyranks.handlers;
// Example (not implemented)

/*
import com.github.mbto.funnyranks.common.dto.Message;
import lombok.extern.slf4j.Slf4j;
import org.jooq.types.UInteger;
import org.jooq.types.UShort;

import java.nio.charset.StandardCharsets;

// 232330	Counter-Strike: Source Dedicated Server
@Slf4j
public class App232330 extends MessageHandler {
    public static final UInteger appId = UInteger.valueOf(232330);

    @Override
    public UInteger getAppId() {
        return appId;
    }

    @Override
    public boolean validate(UShort portValue, byte[] data) {
        return true;
    }

    @Override
    public String convert(UShort portValue, byte[] data) {
        return new String(data, StandardCharsets.UTF_8);
    }

    @Override
    public void handle(Message<?> message) {
        throw new UnsupportedOperationException("Example (not implemented)");
    }
}
*/
